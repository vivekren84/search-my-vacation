# R1.2-WS3-IMP-02A-EBC-KEERTHI — WP-5 Functional Validation

**Persona:** Keerthi — Functional Validation Specialist
**Workstream:** Workstream 3 (Destination Intelligence), Phase 2 (Generation & Vocabulary Alignment)
**Subject:** WP-5 — Controlled Vocabulary Synchronisation
**Validated against:** `R1.2-WS3-IMP-02-EBC-RAD-WP5-Implementation.md` (Rad's WP-5 Engineering Completion Report)
**Repository:** `/Users/viveksophu/Documents/Projects/SearchMyVacation`
**Branch:** `feature/ebcr1.2-003-trust-strip-visual-refresh` (uncommitted working-tree state, baseline commit `a183e18`)
**Workbook checksum confirmed:** `868271e5d094d0c0915f8963da57cce6b57a37d432b0c14869867791c0bc1047` (independently recomputed — matches Rad's report)
**Validation date:** 19 August 2026
**Scope note:** functional validation only. No source code or workbook content was modified. All commands below were executed independently; findings were not accepted from Rad's report without direct reproduction.

---

## Phase A — Validation Preparation

**Mandatory reading completed** (this session): `ADR-R1.2-WS3-001` (re-confirmed unchanged since WP-4 read, via file mtime); `EBC-R1.2-03.05` (WP-5 scope, carried over from WP-4); `R1.2-WS3-IMP-02-EBC-RAD-WP5-Implementation.md` (full); `DEC-R1.2-015` (carried over — explicitly covers WP-4 and WP-5); `JOURNEY-INTELLIGENCE-GENERATOR.md` (updated version, full).

**Preconditions confirmed:**

| Precondition | Status | Evidence |
| --- | --- | --- |
| WP-5 implementation complete | ✅ Confirmed | `git status --short` shows the exact file set Rad's report describes (`generateLabelMappings.ts`, `labelMappingSource.ts` new; `release1Candidates.ts`, `writeGenerationReport.ts`, `generateArtifacts.ts`, `generateMetadata.ts`, intelligence `index.ts`/`types.ts` modified) |
| Repository accessible | ✅ Confirmed | Direct `device_bash` access to repo root |
| Branch/commit matches Rad's report | ✅ Confirmed | `feature/ebcr1.2-003-trust-strip-visual-refresh`, baseline `a183e18` |
| Workbook checksum matches | ✅ Confirmed | Independently recomputed `shasum -a 256`, byte-identical |
| Generator executable | ✅ Confirmed | Executed directly (Phase B) |
| Engineering validation completed | ✅ Confirmed | Rad's report Phase C table reviewed; spot-checked independently (`tsc --noEmit`, `npm run lint`, both clean) |
| No unresolved implementation blockers | ✅ Confirmed | No blocker encountered during independent execution |

---

## Phase B — Generator Execution

**Run 1 — in-place execution** (`npm run generate:journey-intelligence`, output to live `web/generated/`):

- Workbook loaded, checksum matched.
- Validation: 12,925 checks passed, 17 warnings (all pre-existing `REVIEW_REQUIRED`/`DESTINATION_WITHOUT_JOURNEY_BASE` — unchanged from WP-4 baseline).
- KB Reconciliation (WP-4): WARN mode, 24 destinations / 7 member regions checked, 9 findings — identical to WP-4 baseline.
- **Label Mapping generation (WP-5) event observed directly in my own execution's live log:** `"Generating Label Mappings","status":"COMPLETE","comparisonFindings":0,"reachability":["Emotion:58.8%","Theme:44.4%","TravellerType:100%"]`.
- Run then failed at the post-swap backup-directory cleanup step: `EPERM: operation not permitted, unlink '.../.journey-intelligence-backup-38/metadata.json'`.

This is the same device-bridge FUSE limitation identified and proven environment-only (not a functional defect) during WP-4 validation — reproduced here in exactly the same failure shape, at the same pipeline stage, for the same reason (the FUSE-mounted backup directory cannot be unlinked from this bridge). It is not a WP-5 regression.

As with WP-4, the in-place run's atomic directory swap dropped `web/generated/journey-itineraries/` (the pre-existing, separately-generated, unrelated subdirectory — `OBS-R1.2-007`). Restored via `mv` from the run's own backup directory; confirmed via `git status --short -- web/generated/` that only `intelligence-manifest.json` and `metadata.json` remained modified afterward, with zero diff on every other artifact including the restored `journey-itineraries/` subtree. The backup directory was archived to `_to_delete/` rather than deleted (device-bridge cannot `rm`).

**Run 2 — clean isolated execution** (`node .../index.js --output _to_delete/wp5-clean-validation-test/generated --report .../report.md`, avoiding the backup-cleanup step entirely):

- Full pipeline completed end-to-end with **no failure**: Workbook Loaded → Validation Passed (12,925/12,925) → KB Reconciliation Complete (9 findings, WARN) → all 6 core artifacts generated → **Label Mapping generation complete (0 comparison findings; Emotion 58.8%, Theme 44.4%, TravellerType 100%)** → Writing Artifacts Complete (8 artifacts) → Verification Passed (17,455/17,455 checks) → Generation Complete, **determinism: PASS**.
- This confirms the generator executes successfully end-to-end and that the WP-5 failure mode observed in Run 1 is exclusively the pre-existing environment limitation, not a functional defect in WP-5 itself.

**Determinism (independent verification, beyond the generator's own internal 2-execution check):**

Compared every artifact from Run 1 (in-place) against Run 2 (clean output) by SHA-256. The six destination/candidate artifacts (`journey-dna.json`, `compatibility-matrix.json`, `constraint-library.json`, `reason-library.json`, `journey-seeds.json`, `journey-templates.json`) were **byte-identical**. `metadata.json` and `intelligence-manifest.json` differed only in the two fields the generator itself documents as volatile (`generatedAt`, `durationMilliseconds`) — confirmed by `jq 'del(.generatedAt)'` diff showing `metadata.json` fully identical once that field is excluded, and `intelligence-manifest.json`'s only diffs being its embedded `metadata.json` checksum reference (which necessarily changes when `metadata.json`'s timestamp changes) and `durationMilliseconds`.

---

## Phase C — Functional Validation

### C.1 Generated mapping validation

Read `web/generated/metadata.json` directly (post Run 1). Confirmed `labelMappings.emotionByLabel` (11 entries), `labelMappings.themesByLabel` (15 entries), and `labelMappings.travellerByLabel` (5 entries) are present and, byte-for-byte, identical in content to the hand-authored constant tables removed from `release1Candidates.ts` in the same diff (verified via `git diff` — the removed `EMOTION_BY_LABEL`/`THEMES_BY_LABEL`/`TRAVELLER_BY_LABEL` literal objects match the generated values exactly, entry for entry). This independently confirms Rad's claim that "mapping values are unchanged" — not accepted on the strength of the code comment alone.

Confirmed `release1Candidates.ts` now reads these three tables from `runtimeJourneyIntelligence.metadata.labelMappings` rather than declaring them as local constants (diff reviewed directly).

**TRAVELLER_BY_LABEL governance-compliance check:** read `labelMappingSource.ts` and `generateLabelMappings.ts` in full. Confirmed the runtime `TravellerType` union is **not** silently extended toward the KB's approved 9 — it remains the same 5 values, transcribed unchanged, with an explicit code-level governance boundary comment stating that extending it is Open Decision OD-4 and out of this EBC's scope. Cross-checked `RUNTIME_EMOTION_IDS`/`RUNTIME_THEME_IDS`/`RUNTIME_TRAVELLER_TYPE_IDS` in `labelMappingSource.ts` against the live `EmotionId`/`ThemeId`/`TravellerType` unions in `web/lib/journey-director/engine/engine.types.ts` directly: **exact match**, 17/36/5 values respectively, same order, no drift. This matters because these lists are hand-maintained (the file's own header flags this as a manual-sync risk); a drift here would silently corrupt the reachability percentages, so it was verified rather than assumed. **No manual mapping regression found.**

### C.2 Comparison Report

Present in the freshly generated report (Run 2) under `## Controlled Vocabulary Comparison & Reachability (WP-5)`, immediately following the existing `## KB → Operational Reconciliation (WP-4)` section, and confirmed unconditionally rendered — the section header, explanatory text, and Comparison/Reachability subsections are present regardless of finding count (mirrors the WP-4 section's non-omission pattern; also confirmed by inspecting `writeGenerationReport.ts`'s structure, which renders the section unconditionally rather than gating on `comparison.length > 0`).

- **Comparison findings: 0** — reported as "None. Every operational-layer Traveller Type/Emotional Goal/Desired Experience label has a governed runtime-ID mapping, and every governed mapping entry has a matching operational-layer label."
- This zero-finding result was independently sanity-checked, not taken at face value: the operational layer's record counts (`emotionalGoals: 11`, `desiredExperiences: 15`, `travellerTypes: 5` per `metadata.json`'s `recordCounts`) match exactly the entry counts in the governed mapping tables (11/15/5), which is consistent with — and gives confidence in — a genuine zero-finding comparison rather than a comparison that silently short-circuits. The comparison logic itself (`generateLabelMappings.ts`) implements both directions of the check (`LABEL_WITHOUT_MAPPING` and `MAPPING_WITHOUT_OPERATIONAL_LABEL`), confirmed by direct code reading, not merely inferred from zero output.
- Zero-difference reporting is displayed correctly and unambiguously (plain-language sentence, not a bare empty array or silent omission).

### C.3 Reachability Reporting

Present and correctly populated in the same report section:

| Vocabulary | Runtime size | Reachable | Reachable % | KB-approved size |
| --- | ---: | ---: | ---: | ---: |
| Emotion | 17 | 10 | 58.8% | — |
| Theme | 36 | 16 | 44.4% | — |
| TravellerType | 5 | 5 | 100% | 9 |

- Figures match `metadata.json`'s `labelMappings.reachability` array exactly (independently cross-checked, not just read once).
- Unreachable-value lists are enumerated (7 emotions, 20 themes) — matches the RC-6 root-cause figures this report is closing out.
- **TravellerType governance note wording reviewed directly:** correctly and clearly states the runtime type carries 5 values (100% of which are reachable), the KB approves 9, that extending toward the KB's full vocabulary is Open Decision OD-4 (cited with its source document), that OD-4 "remains unresolved," and that this report "makes the gap visible... and does not resolve it." This is an accurate, non-misleading description of the current state — independently confirmed OD-4 is in fact still unresolved by grepping `docs/10-Backlog/RELEASE-1.2.md` for any OD-4/vocabulary-scope decision-log entry: **none found**, consistent with the report's own claim.

### C.4 Metadata / manifest generation

`metadata.json` generation completes and now includes the `labelMappings` object (`emotionByLabel`, `themesByLabel`, `travellerByLabel`, `comparison`, `reachability`) alongside its pre-existing fields — confirmed by direct inspection of the generated file, both from the in-place run and the clean run. `intelligence-manifest.json` generation completes normally; its only diffs against the pre-run baseline are the expected volatile fields.

---

## Phase D — Regression Validation

Independently re-ran the adjacent verification suite (not accepted from Rad's report alone):

| Check | Result | Notes |
| --- | --- | --- |
| `npx tsc --noEmit` (web app) | ✅ Clean, zero errors | |
| `npm run lint` | ✅ Clean, zero warnings/errors | |
| `npm run verify:journey-catalogue` | ✅ Passed (49 checks) | **22 active candidates; 4 governed exclusions** — identical to WP-4 baseline, candidate counts unchanged |
| `npm run verify:journey-runtime` | ✅ Passed (152 checks) | Qualified shortlist `perfect-match:Bali, different-rhythm:Malaysia, pleasant-surprise:Thailand` — identical to WP-4 baseline |
| `npm run verify:journey-orchestration` | ✅ Passed (59 checks) | |
| `npm run verify:journey-presentation` | ✅ Passed (37 checks) | |
| `npm run verify:journey-presentation-catalogue` | ✅ Passed (5 entries, 0 hard failures) | 3 known-deferred warnings, unrelated to WP-5 (Sri Lanka/Vizag region-ID mismatches, pre-existing) |
| `npm run verify:journey-leads` | ✅ Passed (104 checks) | |
| `npm run verify:journey-intelligence` (standalone artifact check) | ❌ Fails, as expected | Reproduces the pre-existing "exactly eight documented artifacts" vs. 9-with-`journey-itineraries/` failure — same message, same cause (`OBS-R1.2-007`). Not a WP-5 regression. |
| `npm run verify:journey-intelligence:steering` | ❌ Fails, as expected | Reproduces the pre-existing Goa/mountain-intent assertion failure byte-identically. Not a WP-5 regression. |
| `npm run verify:journey-engine` | ❌ Fails, as expected | Reproduces the pre-existing `moodValues`/escape-preselection assertion failure byte-identically. Not a WP-5 regression. |

**Artifact-level regression check:** SHA-256 comparison (Phase B) confirmed the six destination/candidate artifacts are byte-identical between the in-place and clean-output runs, and `metadata.json`/`intelligence-manifest.json` differ only in documented volatile fields. `git diff --stat -- web/generated/journey-itineraries/` shows zero diff after restoration.

**Repository hygiene check:** final `git status --short` after all validation activity matches the pre-validation baseline exactly (no residual changes, no leftover backup directories in `web/`, no orphaned generated output outside `_to_delete/`).

No new functional regression was introduced by WP-5. All three known pre-existing failures reproduce identically to the WP-4 baseline; none are worsened, and none are newly introduced by WP-5.

---

## Acceptance Criteria Matrix

| # | Acceptance Criterion | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Generator executes successfully | ✅ Passed | Clean end-to-end run (Run 2); in-place run failure isolated to pre-existing environment limitation (Run 1) |
| 2 | Label-mapping generation completes | ✅ Passed | `"Generating Label Mappings","status":"COMPLETE"` observed directly in both runs |
| 3 | Comparison reporting produced | ✅ Passed | `## Controlled Vocabulary Comparison & Reachability (WP-5)` section present, unconditional, 0 findings correctly displayed |
| 4 | Reachability reporting produced | ✅ Passed | Emotion 58.8%, Theme 44.4%, TravellerType 100%/KB-9 — all independently cross-checked against `metadata.json` |
| 5 | Metadata generation completes | ✅ Passed | `metadata.json` includes `labelMappings` object in both runs |
| 6 | Manifest generation completes | ✅ Passed | `intelligence-manifest.json` generated normally in both runs |
| 7 | EMOTION_BY_LABEL generated | ✅ Passed | 11 entries, byte-identical to prior hand-authored table |
| 8 | THEMES_BY_LABEL generated | ✅ Passed | 15 entries, byte-identical to prior hand-authored table |
| 9 | TRAVELLER_BY_LABEL handling matches approved governance | ✅ Passed | Stays at 5 values; OD-4 extension explicitly not performed; gap correctly reported, not resolved |
| 10 | No manual mapping regression | ✅ Passed | Generated values identical to removed manual constants; runtime-ID unions verified against live `engine.types.ts`, no drift |
| 11 | Generated runtime artefacts functionally unchanged | ✅ Passed | 6/8 artifacts byte-identical; other 2 differ only in documented volatile fields |
| 12 | Candidate counts unchanged | ✅ Passed | 22 active / 4 governed exclusions — identical to WP-4 baseline |
| 13 | Generator outputs deterministic | ✅ Passed | Cross-run SHA-256 comparison + generator's own internal determinism check, both PASS |
| 14 | No unexpected runtime behaviour changes | ✅ Passed | Qualified shortlist identical; all adjacent verification scripts pass or fail identically to baseline |

**All 14 acceptance criteria: Passed. Zero Failed, Blocked, or Not Applicable.**

---

## Functional Findings

None. No genuine functional defect was found in the WP-5 implementation.

---

## Regression Findings

| Finding | Classification | Notes |
| --- | --- | --- |
| Device-bridge FUSE `EPERM` on in-place backup-directory cleanup | Environment issue, pre-existing | Reproduces the identical WP-4 finding; not a WP-5 defect; proven via clean-output run |
| `journey-itineraries/` dropped on in-place atomic swap | Pre-existing, already registered (`OBS-R1.2-007`) | Restored without incident; zero diff after restoration |
| `verify:journey-intelligence` standalone artifact-count failure | Pre-existing, already registered | Byte-identical reproduction, unrelated to WP-5 |
| `verify:journey-intelligence:steering` Goa/mountain-intent failure | Pre-existing, already registered | Byte-identical reproduction, unrelated to WP-5 |
| `verify:journey-engine` moodValues/escape-preselection failure | Pre-existing, already registered | Byte-identical reproduction, unrelated to WP-5 |

No new regression was introduced by WP-5.

---

## Observations (informational only — not defects, not investigated further per this EBC's Out of Scope)

- `docs/10-Backlog/RELEASE-1.2.md` has not yet been updated to reflect WP-5's completion (its Section 5 Phase 2 dashboard still reads "WP-5 ... Not Started, not yet authorised" as of its last recorded update). This is expected — tracker consolidation is a Tiger Stage 10 activity, not something WP-5 implementation or this validation touches, and does not affect any acceptance criterion above.
- OD-4 (Traveller Type vocabulary scope) remains open, as it did after WP-4. This is unchanged, expected, and explicitly out of this EBC's scope — recorded here only for continuity with the WP-4 report's equivalent note.

---

## Phase E — Final Recommendation

## 🟢 Passed — Ready for Tiger Consolidation

All 14 acceptance criteria were independently verified and passed. No functional defects were found. All regression findings are pre-existing, already-registered observations (`OBS-R1.2-007`/`008`/`009` plus the same environment-only FUSE limitation already documented under WP-4), reproduced byte-identically and not worsened. Generator execution, label-mapping generation, comparison reporting, reachability reporting, metadata/manifest generation, and runtime preservation were all confirmed with direct, independently-reproduced evidence — not accepted from the engineering report alone.

This completes independent functional validation of both WP-4 and WP-5. On this basis, Workstream 3 Phase 2 (Generation & Vocabulary Alignment) is functionally ready for Tiger's consolidation and Release 1.2 final-consolidation sequencing, subject to Tiger's own review of the Promotion Review Checklist items (ADR §9) and any outstanding project-owner decisions (OD-4 and others) that remain open independent of this validation's scope.

No code changes were made as part of this EBC.
