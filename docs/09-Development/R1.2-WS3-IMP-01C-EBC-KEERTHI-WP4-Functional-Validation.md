# R1.2-WS3-IMP-01C-EBC-KEERTHI — WP-4 Functional Validation

**Persona:** Keerthi (Functional Validation Specialist)
**Workstream:** WS3 — Destination Intelligence | Phase 2 — WP-4 Functional Validation
**Repository root:** `/Users/viveksophu/Documents/Projects/SearchMyVacation` (local access via device bridge, confirmed available)
**Branch:** `feature/ebcr1.2-003-trust-strip-visual-refresh`
**Date:** 19 August 2026
**Scope:** Functional validation only. No architecture, implementation approach, workbook authoring, or engineering root-cause investigation performed. No code was modified. Generated artefacts (the generator's own output) were regenerated by running the approved generator as this EBC's own validation activity requires — see Session Notes.

---

## Session Startup Confirmation

- Requested task: WP-4 functional validation, per this EBC.
- Active persona: Keerthi.
- Required reading completed in full: `ADR-R1.2-WS3-001-Destination-Knowledge-Governance.md`, `EBC-R1.2-03.05-RAD-Destination-Intelligence-Implementation-Planning.md`, `R1.2-WS3-IMP-01A-EBC-RAD-WP4-Implementation.md`, `R1.2-WS3-IMP-01B-EBC-RAD-Workbook-Consistency-Remediation.md`, `DEC-R1.2-015-Ratification-Warn-Mode-First.md`.
- Repository root confirmed via device bridge; branch confirmed (`feature/ebcr1.2-003-trust-strip-visual-refresh`); working-tree status inspected before and after all activity (see Session Notes).
- Preconditions checked, not assumed: latest WP-4 implementation present (uncommitted, as Rad's reports describe); workbook remediation completed (checksum `868271e5...` independently confirmed matching 01B); generator executes (independently run, see below); engineering validation completed (lint/tsc/build already reported by Rad — not re-run here, consistent with this EBC's framing that WP-4's own engineering validation is settled).

---

## Session Notes (read before the results)

- **A naming mismatch, named per Project Instructions Section 35, not resolved silently:** this EBC's required-reading list cites `ADR-R1.2-WS3-001` without a path. The file actually lives at `docs/20-Architecture/ADR-R1.2-WS3-001-Destination-Knowledge-Governance.md` (not `docs/03-ADR/`, which contains only two empty placeholder files). Located and read in full; no impact on this validation.
- **Generated artefacts were regenerated during this validation, deliberately.** This EBC's own Validation Scope requires confirming "generation is not blocked," "reconciliation report is generated," and "generated artefacts are produced" — none of which can be validated by reading code or a prior report alone (Project Instructions Section 29: "a runtime requirement cannot pass through code inspection alone"). I therefore ran `npm run generate:journey-intelligence` and a second, isolated invocation myself, against the existing, already-remediated workbook, and did not otherwise touch business logic, workbook content, or architecture.
- **Environment finding, independently confirmed, matching Rad's `R1.2-WS3-IMP-01B` Finding 1 exactly:** on this device-bridge mount, `npm run generate:journey-intelligence` run against the existing `web/generated/` directory reports a process failure — `EPERM: operation not permitted, unlink '.../.journey-intelligence-backup-38/journey-seeds.json'` — during the post-write backup-cleanup step, after every generation step (including KB reconciliation and all artifact writes) had already completed. This is a device-bridge FUSE-mount characteristic (the same class of limitation this whole engagement has repeatedly hit when deleting files through this bridge), not a WP-4 defect. I proved this precisely rather than accepting Rad's characterisation on trust: invoking the generator a second time with a fresh, not-yet-existing `--output` directory (so no backup-cleanup step is needed at all) completed **fully clean, end to end**, including a freshly-written generation report. This is the run all evidence below is drawn from.
- **One incidental, already-documented side effect reproduced and corrected.** Running the generator against the existing `web/generated/` directory (before the clean-output test above) reproduced `R1.2-WS3-IMP-01B`'s Finding 2 exactly: `web/generated/journey-itineraries/` (owned by a separate, unrelated generator) was dropped by the atomic swap. I restored it from the run's own backup directory (`mv`, the same non-destructive mechanism Rad's report used), confirmed via `git diff --stat` that the restoration produced **zero diff** against the pre-existing tracked state, and moved the now-stale backup directory and my own scratch test-output directory into `_to_delete/` afterward, consistent with this session's established device-bridge housekeeping pattern. No business logic, workbook, or source code was touched by any of this.
- **Out of scope, and not investigated:** architecture, implementation approach, workbook authoring, the `verify:journey-intelligence:steering` "Goa" finding, the `verify:journey-engine` `moodValues` fixture finding, and generated-artefact ownership (the `journey-itineraries/` shared-directory design). Where these surfaced during regression testing (see below), I independently reconfirmed they reproduce byte-identically to Rad's already-logged descriptions and did not investigate further.

---

## Acceptance Criteria Matrix

| # | Acceptance Criterion | Status | Evidence |
| --- | --- | --- | --- |
| AC-1 | Warn Mode behaves as approved | **Passed** | Generator log and generation report both show `mode: WARN`; report states explicitly "findings are reported and do not block this run; Block Mode is not approved for Release 1.2 Phase 2" |
| AC-2 | Reconciliation findings do not block generation | **Passed** | Both my runs show the KB Reconciliation step (`findings: 9`) completing, followed immediately by Journey DNA/Compatibility/Constraints/Reasons/Seeds/Templates generation all completing normally in the same process |
| AC-3 | The reconciliation section is always generated | **Passed** | Present in this run's report with 9 real findings; additionally confirmed at the code level (`writeGenerationReport.ts`) that the section header and the Promotion Checklist header are unconditional parts of the report template — only the findings' inner bullet list varies by count, never the section's presence |
| AC-4 | Promotion checklist appears | **Passed** | "## Promotion Review Checklist (ADR §9)" section present in the generated report, correctly scoped to ADR §9's Change Authority Matrix, with checklist items that adapt to whether findings are open |
| AC-5 | Existing functionality remains intact | **Passed** | Workbook/artifact/runtime-package validation all PASS (30,380/30,380 checks, 0 failures); determinism PASS (2 executions, 8 artifacts, identical); 5 independently re-run adjacent `verify:journey-*` scripts all PASS with the same check counts Rad reported |
| AC-6 | No functional regressions are identified | **Passed** | See Regression Findings below — the only failing checks are two already-documented, pre-existing issues, reproduced byte-identically and confirmed unrelated to WP-4 |

---

## Test Evidence

### TE-1 — Functional walkthrough: generator execution

**Command:** `npm run generate:journey-intelligence` (against the existing `web/generated/` output).
**Result:** KB Reconciliation step completed (`mode: WARN`, `destinationsChecked: 24`, `memberRegionsChecked: 7`, `findings: 9`); all six generation steps (Journey DNA, Compatibility, Constraints, Reasons, Journey Seeds, Journey Templates) completed afterward; artifact swap-in to `web/generated/` succeeded (confirmed via file timestamps updating from `11:55:56Z` to `13:30:14.787Z`, and `metadata.json`'s `workbookChecksum` field unchanged and correct). Process then failed at the unrelated backup-cleanup step (see Session Notes) — a device-bridge characteristic, not a WP-4 defect.

**Command:** `node .../index.js --output generated-clean-validation-test` (fresh output path, isolating the backup-cleanup step entirely).
**Result:** Fully clean pass. Log shows, in order: Workbook Loaded (PASSED) → Validation Passed (12,925 checks, 17 warnings) → **KB Reconciliation Complete (WARN, 24 checked, 7 member regions, 9 findings)** → six generation steps (COMPLETE) → Writing Artifacts (COMPLETE, 8 artifacts) → Verification Passed (17,455/17,455 checks) → **Generation Complete** (determinism: PASS). This is the direct, first-hand proof that reconciliation findings do not block generation and that the pipeline completes end-to-end with WP-4 active.

### TE-2 — Report inspection

Read the freshly-generated `outputs/ebc-003c-b/JOURNEY-INTELLIGENCE-GENERATION-REPORT.md` in full (timestamped `2026-08-19T13:34:04.023Z`, produced by TE-1's clean run). Confirmed present and understandable:
- A "## KB → Operational Reconciliation (WP-4)" section, positioned after Warnings and before Determinism, stating its own scope and explicitly noting it is "never omitted, regardless of finding count."
- Mode (`WARN`), counts (24 destinations/collections, 7 member regions), and a findings count (9), each finding listed with a code (`KB_DESTINATION_ABSENT` / `KB_MEMBER_REGION_ABSENT`), its KB section citation, and a plain-English sentence — no jargon, no unexplained codes. The nine findings match exactly what `R1.2-WS3-IMP-01A` already documented as the known gap (Darjeeling, Hyderabad, Vizag, Dubai, Wildlife and its four members) — no new, previously-unreported findings appeared.
- A "## Promotion Review Checklist (ADR §9)" section, four checkbox items, correctly distinguishing "routine content refresh" (Engineering's own authority) from "destination inclusion or vocabulary reach changes" (requires Product & Experience approval per the ADR), with one item's wording changing correctly based on whether findings are open.
- Validation, Determinism, and Runtime Integration sections all present and consistent with the log output.

**Code-level corroboration (not a substitute for the above, an additional check):** `writeGenerationReport.ts` builds its output as a single template string; the reconciliation section header and the promotion checklist header are both outside any conditional — only the inner findings list and one checklist line vary by finding count (`kbFindings`/`kbFindingsOpen`). This confirms AC-3 is true by construction, not only true for today's particular (non-zero) finding count.

### TE-3 — Generated artefact review

All 8 runtime artefacts confirmed present, sized, and checksummed identically between the log output, the report, and the files on disk: `journey-dna.json`, `compatibility-matrix.json`, `constraint-library.json`, `reason-library.json`, `journey-seeds.json`, `journey-templates.json`, `metadata.json`, `intelligence-manifest.json`. `metadata.json`'s `workbookChecksum` (`868271e5...`) matches the canonical workbook's independently-recomputed SHA-256. Determinism verification (2 executions, 8 artifacts compared) reports **IDENTICAL**.

### TE-4 — Regression validation

Independently re-ran five adjacent verification scripts (not merely re-reading Rad's report of them):

| Script | Result | Checks |
| --- | --- | --- |
| `verify:journey-catalogue` | **PASS** | 49 |
| `verify:journey-runtime` | **PASS** | 152 |
| `verify:journey-orchestration` | **PASS** | 59 |
| `verify:journey-presentation` | **PASS** | 37 |
| `verify:journey-leads` | **PASS** | 104 |

All five check counts match `R1.2-WS3-IMP-01B`'s reported figures exactly — no drift, no new failures.

Two pre-existing failures were independently reconfirmed, not newly discovered:
- `verify:journey-engine`: `Error: Verification failed: escape resolves to its editable mood preselection` — byte-identical message to the already-documented `moodValues` fixture issue (5 fixture entries vs. 6 `JOURNEY_FEELINGS`). Out of scope per this EBC; not investigated further.
- `verify:journey-intelligence:steering`: `Error: Verification failed: mountain intent keeps Goa eligible for scoring rather than hard-excluding it` — byte-identical message to the already-documented, already-proven-unrelated Goa steering finding. Out of scope per this EBC; not investigated further.

Neither failure's message, location, or trigger changed from what `R1.2-WS3-IMP-01B` already reported — confirming both are pre-existing and unrelated to WP-4, not regressions introduced by it.

---

## Defects

**None identified within WP-4's own functional scope.**

WP-4's reconciliation check, its report section, and its promotion checklist all behave exactly as specified in `EBC-R1.2-03.05` and ratified by `DEC-R1.2-015`, independently verified by direct execution rather than by trusting the prior engineering reports.

## Regression Findings

**None caused by WP-4.**

| Item | Status | Classification |
| --- | --- | --- |
| Device-bridge FUSE `EPERM` on backup-directory cleanup | Reproduced, environment-only | Not a functional defect — proven not to affect artifact correctness (clean-output run completed fully); recurrence is a standing characteristic of this session's device bridge, already logged by Rad |
| `journey-itineraries/` dropped by the atomic swap | Reproduced, then restored (zero diff) | Pre-existing, already-logged architectural gap (`OBS-R1.2-007`); out of scope for this EBC ("generated artefact ownership") |
| `verify:journey-engine` failure | Reproduced, unchanged | Pre-existing (`OBS-R1.2-009`); out of scope ("moodValues verification fixture") |
| `verify:journey-intelligence:steering` failure | Reproduced, unchanged | Pre-existing (`OBS-R1.2-008`); out of scope ("Goa steering issue") |

None of the above are new, none are worse than previously documented, and none originate in WP-4's own code (`kbApprovedPortfolio.ts`, `validateKbReconciliation.ts`, or the two files WP-4 modified).

---

## Validation Summary

WP-4 (KB → Operational Reconciliation) functions exactly as specified and ratified. Independently executing the generator — twice, including one fully clean end-to-end run with no environment interference — confirms: the reconciliation check runs in Warn Mode and never blocks generation; the reconciliation report section is always present, whether or not findings exist; the report is understandable, correctly attributing each finding to a KB citation in plain language; the Promotion Review Checklist appears and is correctly scoped to ADR §9; existing generation behaviour, runtime artefacts, and five adjacent functional-verification surfaces are all intact; and the two script failures present in this environment are proven, not merely asserted, to be pre-existing and unrelated to WP-4.

**Answer to this EBC's core question:** yes, WP-4 behaves according to the approved requirements.

## Recommendation

**Passed — Ready for Tiger Consolidation**

All six acceptance criteria are met, independently verified through direct execution rather than code inspection alone. No defects were found within WP-4's scope. The environment-only device-bridge cleanup limitation and the three already-logged, out-of-scope pre-existing issues (`OBS-R1.2-007/008/009`) do not block this recommendation — they are unrelated to WP-4's functional correctness and are already tracked for separate Engineering attention per `R1.2-WS3-IMP-01B`'s Cross-Reference. Recommend Tiger route this validation into Workstream 3's consolidated status alongside the existing WP-4/Workbook-Remediation chain.
